<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Work_winui_.User.Searchpage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Work_winui_.User"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">    
        <Grid>
        <StackPanel  Margin="40,20,40,0">
            <TextBlock Text="SEARCH" FontSize="32" FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,0,30"/>

            <StackPanel Width="500" HorizontalAlignment="Center" Margin="0,0,0,20" x:Name="SearchCriteriaPanel">
                <!-- First search row -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <ComboBox x:Name="SearchField1" Width="150" Margin="0,0,10,0" VerticalAlignment="Center" SelectedIndex="0">
                        <ComboBoxItem Content="Job id"/>
                    </ComboBox>
                    <TextBox x:Name="SearchValue1" Width="250" PlaceholderText="ENTER JOB ID" VerticalAlignment="Center"/>
                    <Button x:Name="AddCriteriaButton" Content="+" Click="AddCriteriaButton_Click" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel x:Name="OrSeparator" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,5" Visibility="Collapsed">
                    <Rectangle Height="1" Fill="Gray" Width="195" />
                    <TextBlock Text="+" Margin="10,0" VerticalAlignment="Center"/>
                    <Rectangle Height="1" Fill="Gray" Width="180" />
                </StackPanel>

                <!-- Second search row (initially hidden) -->
                <StackPanel x:Name="SecondSearchRow" Orientation="Horizontal" Margin="0,10,0,10" Visibility="Collapsed">
                    <ComboBox x:Name="SearchField2" Width="150" Margin="0,0,10,0" VerticalAlignment="Center" PlaceholderText="Select...">
                        <ComboBoxItem Content="Job id"/>
                        <ComboBoxItem Content="Customer Name"/>
                        <ComboBoxItem Content="Address"/>
                    </ComboBox>
                    <TextBox x:Name="SearchValue2" Width="250" PlaceholderText="ENTER JOB ID" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>

            <Button Content="SEARCH" Width="120" Height="40" Margin="0,30,0,20" HorizontalAlignment="Center"/>

            <Border BorderThickness="1" BorderBrush="LightGray" Background="#FFF0F0F0" CornerRadius="5" Height="400" Margin="0,20,0,30">
                <!-- Content for search results -->
            </Border>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</UserControl>
